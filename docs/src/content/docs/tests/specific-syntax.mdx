---
title: Specific syntax MDX tests
slug: tests/specific-syntax-mdx
---

import { Image, Picture } from 'astro:assets'
import starlightLight from '../../../assets/tests/starlight-light.png'

An image using the `<Image>` component:

<Image src={starlightLight} alt="Starlight logo" />

An image using the `<Picture>` component:

<Picture src={starlightLight} alt="Starlight logo" />
